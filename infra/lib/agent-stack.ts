/**
 * WickAgent — Bedrock AgentCore Runtime / Memory / Identity, Bedrock model
 * access, the Knowledge Base over S3 Vectors.
 *
 * Alarms: p99 latency breaches, and ANY guardian.error. A Guardian that throws
 * must fail closed AND wake someone (§11).
 */
export {};
