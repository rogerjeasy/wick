/**
 * Screen-reader state, via the com.amazon.devconf.privilege.accessibility
 * privilege declared in manifest.toml.
 *
 * Read AmazonAppDev/vega-sports-app/src/services/a11y before writing this —
 * it ships a working hook and a HintBuilder for list-navigation hints.
 *
 * Vega exposes no contrast or reduced-motion preference (Q7). Wick therefore
 * asks the household once at setup. That gap is a feature request.
 */
export {};
