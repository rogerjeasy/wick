/**
 * ESLint rules for calm 10-foot UI, intended to sit alongside
 * @amazon-devices/eslint-plugin-kepler.
 *
 * Planned rules:
 *   no-auto-dismiss        flag setTimeout-driven unmount of a card surface
 *   no-red                 flag red hex/rgb values in TV component styles
 *   require-focus-order    flag focusable elements with no explicit ordering
 *   min-font-size          flag fontSize below tokens.type.bodyPt
 */
export default { rules: {} };
