/**
 * Strands tools. Typed with Zod schemas (@strands-agents/sdk v1.x).
 *
 * INV-6: every tool is idempotent and carries correlationId.
 * Keep tool surfaces narrow — an agent with ten tools is four agents wearing a
 * trench coat.
 */
export {};
